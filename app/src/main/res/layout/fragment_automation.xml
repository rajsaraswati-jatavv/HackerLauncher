<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    android:padding="8dp">

    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnCount="3"
        android:rowCount="3">

        <Button android:id="@+id/btnRecord" android:text="Record" style="@style/HackerButton" />
        <Button android:id="@+id/btnStopRecord" android:text="Stop Rec" style="@style/HackerButton" />
        <Button android:id="@+id/btnPlayMacro" android:text="Play" style="@style/HackerButton" />
        <Button android:id="@+id/btnClearMacro" android:text="Clear" style="@style/HackerButton" />
        <Button android:id="@+id/btnSchedule" android:text="Schedule" style="@style/HackerButton" />
        <Button android:id="@+id/btnAutoRecon" android:text="Auto Recon" style="@style/HackerButton" />
        <Button android:id="@+id/btnNotifLog" android:text="Notif Log" style="@style/HackerButton" />
        <Button android:id="@+id/btnListTasks" android:text="Tasks" style="@style/HackerButton" />
    </GridLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="4dp">

        <EditText
            android:id="@+id/etTaskName"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:hint="Task name"
            android:textColorHint="#005500"
            android:textColor="#00FF00"
            android:background="#0A1A0A"
            android:fontFamily="monospace"
            android:textSize="12sp"
            android:padding="8dp"
            android:singleLine="true" />

        <EditText
            android:id="@+id/etTaskDelay"
            android:layout_width="80dp"
            android:layout_height="36dp"
            android:hint="Min"
            android:textColorHint="#005500"
            android:textColor="#00FF00"
            android:background="#0A1A0A"
            android:fontFamily="monospace"
            android:textSize="12sp"
            android:padding="8dp"
            android:singleLine="true"
            android:inputType="number" />
    </LinearLayout>

    <ScrollView
        android:id="@+id/scrollViewAuto"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp">

        <TextView
            android:id="@+id/tvAutoOutput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#00FF00"
            android:fontFamily="monospace"
            android:textSize="11sp" />
    </ScrollView>
</LinearLayout>
